const SampleComponent = () => {
    return (
      <div>
        <p className="text-blue-500">Hello, this is a sample component using Tailwind CSS!</p>
      </div>
    );
  };

export default SampleComponent;